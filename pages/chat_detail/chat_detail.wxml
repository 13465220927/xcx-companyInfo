<i-message id="message" />
<view>
    <friend_panel friendImg="{{img}}" conversationData="{{conversationData}}"></friend_panel>
    <my-panel name="{{name}}" img="{{img}}" bind:postMessage="postMessage" friendId="{{friendId}}"></my-panel>
</view>