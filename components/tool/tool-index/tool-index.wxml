<view class="tools-wrap">
    <view data-index="{{index}}" bindtap="toUrl" class="item" wx:for="{{toolsData}}" wx:key="{{index}}">
        <image src="{{item.img}}" />
        <text class="tool-name">
            {{item.name}}
        </text>
    </view>
   
</view>
