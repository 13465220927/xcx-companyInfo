<view class="body-wrap">
     <view class="info-header">
         <i-icon type="praise" size="18" color="#2b3135" />
         企业简介
     </view>
     <view class="info-detail">
        {{company_info}}
     </view>
       <view class="info-header">
             <i-icon type="integral" size="18" color="#2b3135" />
             资质荣誉
      </view>
      <view class="prizeImg">
          <image wx:for="{{honor_photo}}" wx:key="{{index}}" src="{{hostname}}{{item}}" />
     </view>
      <view class="info-header">
             <i-icon type="picture" size="18" color="#2b3135" />
             产品照片
      </view>
      <view class="pictureImg">
          <image wx:for="{{product_photo}}" wx:key="{{index}}" src="{{hostname}}{{item}}" />
     </view>
</view>